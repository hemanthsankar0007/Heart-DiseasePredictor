# Environment variables for Heart Disease Prediction App
# Copy this file to .env in the respective directories and update the values as needed

# ===================
# Frontend Environment Variables (frontend/.env)
# ===================
# Backend API URL - the frontend will use this URL to call the backend
# For local development, use localhost
VITE_API_URL=http://localhost:8000

# For production deployment, comment out the above and use your deployed backend URL
# VITE_API_URL=https://your-backend-domain.com

# Note: If VITE_API_URL is not set, the app will use the hardcoded API_URL 
# in App.jsx (currently set to https://heart-diseasepredictor.onrender.com)

# ===================
# Backend Environment Variables (backend/.env)
# ===================
# Frontend URL for CORS - allows the backend to accept requests from this origin
FRONTEND_URL=http://localhost:5173

# Environment type
ENVIRONMENT=development

# Port configurations
PORT=8000
FRONTEND_PORT=5173
